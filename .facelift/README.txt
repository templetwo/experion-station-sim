Facelift before/after screenshot sets (gitignored working area).
  before/  captured from v3 d6954a0, the shipped light look
  after/   same default look after the night-palette work (unchanged by design --
           the default palette is still `representative`, per the fallback rule)
  night/   the `night` palette forced as default, to show what it does today:
           alarm colours only. The dark CHROME layer is not built yet, which is
           why this render is near-identical to `before`.
