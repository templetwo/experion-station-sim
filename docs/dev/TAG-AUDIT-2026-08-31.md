<!-- @artifact dev -->
# Tag audit counter-sign — do not cut v3.0.0

**Auditor:** Codex, on clean `cf5693a` matching `origin/v3`.
**Counter-sign:** MacBook seat (grok-4.6), 2026-08-31.
**Verdict:** agree. Do not tag.

The suite staying green (708 / 707 / 1 skip) is real and is not evidence the
release gates are closed. Several tests encode the known gaps as passing
expectations. That is the load-bearing meta-finding.

---

## Independent probes this seat ran

A1 after preset, snapshot slot 0, startADrill, settle 75 s, Diagnose, four
accepted TRAINING.* actions (mark, pin-compare, hypothesis FIELD, verify):

| | retained accepted events | score |
|---|---:|---:|
| before replay | 4 | 60 |
| after `startReplay(0)` + `replayToEnd()` | 0 | 0 |

`aDrill.id` is `A1` after replay (the `ADRILL` op re-armed it). Journal ops
present: `ADRILL`, `TRAINING.MARK_EVIDENCE`, `TRAINING.PIN_COMPARE`,
`TRAINING.SUBMIT_HYPOTHESIS`, `TRAINING.VERIFY`. `applyJournalEntry` has cases
for `ADRILL` / `ADRILLEND` and none for the four TRAINING ops, so they are
silently dropped. Gate 3 is falsified.

The existing snapshot/restore test in `tests/drill-arch-fixtures.test.js` only
round-trips a *mid-drill* snapshot (events already inside `P.aDrill`). It does
not replay post-snapshot TRAINING actions. Green is not coverage.

---

## Finding by finding

| # | Auditor | This seat |
|---|---|---|
| 1 | A-drill replay does not reproduce score | **SOUND.** Measured above. |
| 2 | Most fault defs do not produce specified operational symptoms | **SOUND.** Fixtures file header already records engine-only ACK unreachability. A1's reserved `xmtr` path is the opposite problem: live BADPV + shed + PV→0 (`src/models.js:327`) while the drill teaches frozen/GOOD. A12 engine-only cascade was already named on this seat. |
| 3 | Curriculum not fully completable | **SOUND.** Page never calls `TRAINING.DEBRIEF`. `endADrill` nulls `P.aDrill`. `archSynthEvent` emits MODE.SET / POINT.SUPPRESS / ACK, not INTERLOCK.DEFEAT. A5/A12 gates are module-tested with fabricated events. Clean-run fixtures: only A1 can reach 90 (with ACK); A2–A12 sit 60–70. |
| 4 | Debrief is a shell | **SOUND.** `archDebriefView` passes no score, `archFaultTimeline` is currently-active only, rendered rows drop process samples. |
| 5 | Snapshot is version-marked, not the §4 schema | **SOUND.** `makeSnapshot` still emits `t` / `randState` / `P,L,V` / `journalSeq`. `snapshotData` does not pass `state.archProfile`; `architecture.profile` defaults to `console` via `architectureFromProcess`. `restoreSnapshot` never applies `snap.architecture`. Architecture state that round-trips does so because it is duplicated inside `P`. |
| 6 | No `ESS.Sel`; A-drill lifecycle bypasses dispatch | **SOUND as a gap, with one spec nuance.** V3-PLAN §4 said existing v2 mutations are not rewritten this release (strangler). It also said everything *v3 adds* goes through dispatch, including drill lifecycle and snapshot restore. `startADrill` / `aDrillFire` / `restoreSnapshot` do not. Whole-tree search: no `ESS.Sel`. |
| 7 | Offline A-drill E2E absent | **SOUND.** `tools/smoke.sh` screenshots both builds with DNS blocked. No drill interaction. |

§2 rows 1, 5, 7 (mechanical provenance): agree MET.
§2 rows 2, 3, 4, 6: agree PARTIAL.
§9: S0/SA/S1 MET; S2/S3 partial; S4/S5 not closed.
§11: gate 4 MET; gate 3 FAIL; 1, 2, 5 partial.

---

## What this seat overclaimed

CHANGELOG 3.0.0 said "Stages S0 through S5 of V3-PLAN are landed." That was
wrong. S0–S1 are landed. S2–S3 have artifacts. S4 exit condition (replay
equality, complete Debrief, snapshot contract, coverage ticks, help/philosophy)
is not met. S5 tag is not cut. Corrected beside this file.

The earlier Codex brief (`docs/dev/CODEX-TASKS-2026-08-30.md`) put coverage
ticks and Alarm Help first. That order is superseded: those are real leftover
S4 items, but they are not the tag blockers. Replay equality and live fault
effects are.

---

## Ruling still needed from Anthony (do not guess)

Live fault effects for A2/A4–A12 require either:

- a new physics hook in `src/models.js` (conflicts with addendum D1: non-reserved
  engine faults never touch models; also risks moving S0 goldens), or
- an engine-to-symptoms bridge that is not `models.js`, or
- leaving engine-only drills honest and rewriting their objectives so they do
  not claim process alarms they cannot raise.

A1's BADPV/shed vs "frozen GOOD" is the reserved-`xmtr` path doing what D1
*xmtr* does. Teaching GOOD-quality freeze on that pair is a contradiction in
the drill definition, not only a missing overlay.

Do not start a models.js pass without that ruling.

---

## Next implementation order (agreed)

Uncontested first, no D1 conflict:

1. **Replay TRAINING.*** through `applyJournalEntry` (and inspect-state so
   MARK_EVIDENCE/VERIFY do not fail-closed on replay). Test: the probe in this
   file, as a committed test, must stay red until the score matches.
2. **Debrief completable:** page calls `TRAINING.DEBRIEF`; do not destroy the
   event array before a record is written; pass score + durable fault timeline
   + process samples into `archDebriefView`.
3. **INTERLOCK.DEFEAT** from a real UI action, or drop those gates from A5/A12
   until a real action exists. Fabricated events are not a live gate.

Then, after the ruling:

4. Live fault effects / passable A-drills (honest symptoms, or honest
   objectives).
5. Snapshot schema + restore of `architecture` + `ESS.Sel`.
6. Offline A-drill smoke.
7. Coverage ticks + Alarm Help/philosophy (the previous Codex list).

Do not cut `v3.0.0` until gate 3 is no longer falsified by the A1 replay probe.
