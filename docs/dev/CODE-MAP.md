# Code map: `Experion Station Simulator.dc.html`

Structural map produced 2026-08-29 (v1.1.1, 1798 lines). Line numbers drift as the file changes; use them as anchors, then grep. Single `.dc.html` page: HTML template inside `<x-dc>` (lines 9–903) + one `<script type="text/x-dc" data-dc-script>` (904–1796) holding `class Component extends DCLogic` (905–1795). Runtime is `support.js` (generated bundle, do not edit; the only local patch is the SVG `tspan` fix documented in CHANGELOG 1.1.1).

## 0. Runtime / `DCLogic` base class API (support.js)

```js
// support.js:819
var StreamableLogic = class {
  constructor(props) { this.props = props || {}; this.state = {}; }
  setState(update, cb) {}   // delegates to host: patch = fn(prev) or object; SHALLOW merge; re-renders
  forceUpdate() {}
  componentDidMount() {}    // fires after mount (and again after a hot logic swap)
  componentDidUpdate(_prevProps) {}
  componentWillUnmount() {}
  renderVals() { return {}; } // the flat object the template renders against (merged over props)
};
```

Facts:
- The script is evaluated with `new Function("DCLogic","StreamableLogic","React", src)` and must define a top-level `class Component extends DCLogic`.
- `setState` does a shallow merge and re-renders. **Mutating instance fields (`this.L`, `this.P`, `this.alarms`) does NOT re-render**; the UI is pumped at 2 Hz by `tick()` calling `setState({tk,blink})`.
- Render: `vals = {...userProps, ...renderVals()}`. A throwing `renderVals()` shows a red overlay.
- Lifecycle names are React-style only (`componentDidMount`, `componentDidUpdate`, `componentWillUnmount`). No timers are provided; the app owns `setInterval`.
- `data-props` on the script tag declares editor-editable props merged into `this.props` (`highColor`, `audible`, `showBadge`).
- Template directives: `sc-if`, `sc-else`, `sc-for list="{{ }}" as="x"` (`$index` available), `hint-placeholder-count` (streaming only). Attributes: `class`→className, `on*` → React handlers (`onClick`, `onDoubleClick`, `onChange`, `onKeyDown`, `onMouseDown`...), `style-hover="css"` / `style-active="css"` generate pseudo-class CSS, everything else (incl. `ref`, `value`, `checked`) passes straight to React. `{{ expr }}` interpolation is a property path resolved against the render object (no JS expressions); inside SVG `<text>` it renders as `<tspan>` (runtime patch 1.1.1).
- Handlers are closures created fresh inside `renderVals()` and referenced by name in the template (`onClick="{{ p.click }}"`).

## 1. Template map (lines 9–903)

| Lines | Region | Key bound names |
|---|---|---|
| 1–8 | head, loads `./support.js` (line 6) | |
| 11–20 | `<helmet><style>` global CSS | |
| 21 | root container (fixed, flex column, Tahoma 12px) | `rootMove`, `rootUp` (faceplate drag) |
| 23–43 | menu bar: `sc-for menus as m`, nested `m.items` | `menus[].{name,bg,fg,toggle,open,items[].{sep,label,cb}}`, `showBadge` |
| 45–52 | toolbar `sc-for tbtns` | `tbtns[].{sep,label,title,bg,cb}` |
| 54–64 | command zone, message zone, alarm line, priority counters | `cmdVal,onCmd,onCmdKey,msgTxt,al.{bg,fg,txt,click},cU,cH,cL,hcol` |
| 66–67 | main row (`onMouseDown="{{ closeMenus }}"`), left pane; displays are absolutely positioned siblings | |
| 69–164 | U1 graphic (`sc-if isG1`): unit tabs 71–77, SVG viewBox 0 0 1420 720, equipment 89–139, valves `sc-for vlist` 141–147, point boxes `sc-for gvList` 149–160 | `utabs`, `gfx.{tankY,tankH,drumY,drumH,pumpFill,pumpGlyph,rTripOp,ovfOp,psvOp}`, `vlist[].{x,y,rot,fill,label,pos,tx,ty}`, `gvList[].{x,y,tag,tagX,tagY,pvX,pvY,pvT,euX,euT,modeX,modeT,bd,bw,hatchOp,aX,aY,aBg,aBd,aFg,aTx,aTy,aTxt,click,dbl}` |
| 166–248 | U2 graphic (`sc-if isG2`): batch header + SCM202 phase pill + START/HOLD/ABORT 168–179, phase list `batch.phaseRows` 217–219, monomer bar 220–225, valves `vl2`, points `gv2` | `batch.{phase,phBg,phFg,pt,startCb,holdCb,holdT,abortCb,phaseRows}`, `gfx2.{lvlY,lvlH,agFill,agFg,tripOp,cmW,cmFill,cmT,cmWarn}` |
| 250–319 | U3 graphic (`sc-if isG3`): flames 274–276, bed glow 288–289, valves `vl3`, points `gv3` | `gfx3.{flameOp,bedGlow,tripOp}` |
| 321–347 | Alarm Summary (`sc-if isAlarms`): filter chips 325–327, buttons 329–331, hard-coded header row 334 (26 icon / 80 Time / 80 Source / 90 Condition / 70 Priority / flex Description / 80 Value / 64 Units / 46 Ack), rows `sc-for av.rows` 337–341, footer 343–345 | `av.{filters,btns,rows[].{icon,t,tag,cond,prio,desc,val,eu,ackT,bg,fg,sel,click,dbl},selInfo,countTxt}` |
| 349–369 | Event Summary (`sc-if isEvents`): filters, header 359 (132 Date/Time, 86 Type, 84 Source, flex Desc, 90 Old, 90 New, 60 Level, 56 Station) | `evFilters`, `evR[].{t,type,src,desc,oldV,newV,lvl,fg}` |
| 371–383 | Message Summary (`sc-if isMsgs`) | `msgsR[].{t,txt}`, `msgsEmpty` |
| 385–426 | Trend (`sc-if isTrend`): group chips, span chips, chart SVG 1060x430 (400–412), legend 414–424 | `tv.{title,groups,spans,gridY,gridX,pens[].{pts,color,dash,label,desc,val,eu,range}}` |
| 428–562 | Point Detail (`sc-if isDetail`): header 430–435, tabs 436–441, MAIN 444–473 (`dpt.mainRows`, mode buttons 462–464), ALARMS 475–496 (`dpt.almRows`), LOOP TUNE 498–516 (PID equation text at 514), CHART 518–536, CONTROL MODULE 538–558 | `dpt.{tag,desc,cm,kindT,tabs,isMain,isAlm,isTune,isChart,isCm,mainRows[].{label,param,value,note,editing,notEditing,cur,und,cb,rowBg},almRows[].{cond,tp,prio,prioFg,state,stFg,note,editing,notEditing,cb,prioCb},tuneRows,chartGrid,chartPens,cmBlocks,cmWires,modes,shedNote,tuneGate,cmNote}` + shared `entryText,entryChange,entryKey,entryRef` |
| 564–584 | System Status (`sc-if isSys`) | `sysPanels[].{title,led,rows[].{k,v}}` |
| 586–691 | Faceplates `sc-for fps as fp` (draggable): PID 596–646, indication 648–667, motor 669–682, shared SILENCE/ACK/DETAIL 684–688 | `fps[].{tag,desc,x,y,z,pin,...,isPid,isInd,isMotor,hiT,loT,pvH,spY,ticks,pvT,spT,opT,opW,indH,eu,spEditing,...,modes,raise,lower,selT,noteT,sil,ack,detail,modeLine,initT,stT,stBg,stFg,cmdT,fbT,permT,permFg,start,stop}` |
| 693–698 | Drill banner (`sc-if db.on`) | `db.{on,tm,end}` |
| 700–829 | Modal dialog (`sc-if dg.open`): logon 709–718, shelve 720–735, drills 737–749, instructor 751–769, debrief 771–789, help 791–800, about 802–808, reset 810–816, sysmenu 818–824 | `dg.{open,title,w,close,isLogon,isShelve,isDrills,isInstr,isDebrief,isHelp,isAbout,isReset,isSysMenu,pw,pwChange,pwKey,pwGo,signOff,shelfTag,reason,reasonChange,durs,shelfGo,drills,randomDrill,faults[].{label,on,state,cb},speeds,resetGo,resetYes,sysLinks,dbName,dbReason,dbRows,dbAsk,dbQ,dbOpts,dbSubmit,dbScored,dbScore,dbGrade}`, `secT` |
| 833–886 | Ops Assistant dock (`sc-if asst.on`): issues 842–864 with steps + GO, ask box 865–873, hits 874–879 | `asst.{on,close,issues[].{sev,sevBg,sevFg,title,why,arrow,open,click,steps[].{n,txt,hasGo,go}},q,qChange,chips,hits,hitsNone}` |
| 890–901 | Status bar | `ledSrv,ledCee,spdT,instrOpen,audT,audCb,secT,secCb,dateT,timeT` |
| 904 | `<script data-dc-script data-props="{highColor,audible,showBadge}">` | |

## 2. Component map (lines 905–1795)

### 2.1 React state (line 906)
`{ tk, blink, unit:'U1', assist, assistSel, assistQ, display:'graphic', hist:[{d:'graphic'}], hi:0, sec:'OPER', msg, msgT, cmd, fps:[], entry, entryText, sel, selAlm, aud, silenced, shelfView:'MAIN', evtFilter:'ALL', detailTag:'FIC102', detailTab:'main', tg:'TG01', span:15, menu, dlg, dlgPw, dlgReason, debAns, drill, drag, speed:1 }`
- `display` ∈ graphic|alarms|events|msgs|trend|detail|sys; `sec` ∈ VIEW|OPER|SUPV|ENGR|MNGR; `shelfView` ∈ MAIN|UNACK|SHELVED; `evtFilter` ∈ ALL|ALARM|OPERATOR|SYSTEM; `speed` ∈ 0|1|2|5.
- `fps`: `{tag,x,y,pin}` array, order = z-order. `entry`: `{tag,param}` with param ∈ SP,OP,K,T1,T2,SPHILM,SPLOLM,OPHILM,OPLOLM,SAFEOP,`TP:<COND>`. `drill`: `{def,t0,ti,injected,m,stableFor,reason?}`.

### 2.2 Non-reactive instance fields (`initSim`, 916–946)
- `this.L` tag database (19 points keyed by tag); `this.V` valves `{pos,stuck,fail}` for FV102,TV202,TV301,PV401,LV401,MV211,JV213,FV310,FV311,QV313; `this.P` process state; `this.alarms`, `this.events`, `this.msgs` (newest first); `this.hist` `{tag:[[t,pv,sp,op],...]}` cap 7200; `this.aid`, `this.eid`; `this.iv` interval; `this.kd` keydown listener; `this.actx` AudioContext.

### 2.3 Methods
| Lines | Method | Purpose |
|---|---|---|
| 908–913 | componentDidMount | initSim; `setInterval(()=>this.tick(),500)`; keydown listener |
| 916–946 | initSim | builds L,V,P; clears alarms/events/msgs/hist; seeds PID integrators |
| 949–950 | rank(l) / can(need) | security ranks VIEW0 OPER1 SUPV2 ENGR3 MNGR4; gate with message |
| 951–957 | msgZone, fmt, fT, fD, addEvent(type,src,desc,oldV,newV), prioColor, prioDark | helpers; events capped 600 |
| 959–970 | raiseA(src,cond,prio,val,eu,desc) / clearA(src,cond) | raise (Journal = event only; re-arm inactive record), RTN (remove if acked) |
| 971–972 | hornNew / beep(freq,dur) | horn re-arm; WebAudio beep |
| 975–988 | tick | runs `step(0.5)` `speed` times; expires msg zone; horn by priority; `setState({tk,blink})` |
| 989 | topUnack | highest-priority unacked unshelved alarm |
| 991–1069 | step(dt) | U1 model, valves, measurements, then stepU2, stepU3, pids, scan, shelfScan, drillWatch, history push |
| 1071–1104 | stepU2(dt) | semi-batch reactor + SCM202 CHARGE→HEATUP→FEED→REACT→COOL→DRAIN→IDLE; modeAttr PROGRAM during sequence; trip 110 °C |
| 1105–1121 | stepU3(dt) | fired preheater + fixed bed; trip 480 °C |
| 1122–1133 | seqCmd(cmd) / unitOf(tag) | START/HOLD/ABORT; tag→unit (hard-coded lists) |
| 1135–1181 | diagnose() / topics() | Ops Assistant rules (max 8 issues) and 14 Q&A topics |
| 1182–1203 | pids(dt) | PID for 13 loops (see 2.5) |
| 1205–1210 | applyShed(l) | bad-PV shed NOSHED/SHEDLOW/SHEDHIGH/SHEDSAFE/default hold in MAN |
| 1212–1230 | scan() | alarm limit scan PVHI/PVHH/PVLO/PVLL/DEVHI, deadband 1% span, latch in `l._as`; motor TRIP |
| 1232–1240 | shelfScan() | auto-unshelve on timer |
| 1242–1246 | tripMotor(tag,why) | |
| 1249–1339 | drillDefs, startDrill, injectFault, drillWatch, dHook, dAct, dTrip, endDrill, scoreDrill | drills D1,D2,D3,D4,D6,D9,D11,D12; verbs ACK,OUTFLOW,CUTFEED,MAN202,AUTO401,START,CUTMONO,QUENCH; score ack latency 25 + action 30 + no trip 20 + stabilized 15 + quiz 10 |
| 1342–1363 | nav, goBack, goFwd, parseCmd | navigation history; command parser (U1/U2/U3, ASSIST, tag→detail, ALM/EVT/MSG/TRN/GR/SYS/DETAIL, HELP, DRILL) |
| 1364–1398 | silence, ackAlarm, ackTop, ackPage, visAlarms, shelve(mins), unshelve | |
| 1399–1450 | setMode, openEntry, commitEntry, raiseLower, motorCmd | mode rules (CAS needs master; bad PV locks), clamps to SPHILM/SPLOLM/OPHILM/OPLOLM, events with old/new |
| 1451–1470 | openFp, logon (oper/supv/engr/mngr), onKey (F1 silence, F2 ack, F3 alarm summary, F4 detail) | |
| 1473–1793 | renderVals() | builds the entire view model; returned literal at 1765–1792 |
| 1794 | mmss | |

### 2.4 Point model (`this.L`, 918–940)
PID defaults from factory `P(o)`: `{kind:'pid', mode:'AUTO', modeAttr:'OPERATOR', T2:0, ophilm:100, oplolm:0, opexhi:105, opexlo:-5, safeop:0, shed:'SHEDHOLD', badPv:false, init:false, dec:1, _as:{}}`.
PID fields: `tag, desc, eu, lo, hi, dec, kind, pv, sp, op, I, lastPv, K, T1, T2, act:'DIR'|'REV', mode, modeAttr, master, slave, init, sphilm, splolm, ophilm, oplolm, opexhi, opexlo, safeop, shed, badPv, cm, alm:{COND:[tripPoint, priority]}, _as:{COND:bool}`.
Indication points (`kind:'ind'`): FI100, PI214, LI215, TI312. Motor points (`kind:'motor'`): P101, M202 with `run,cmd,lock,trip,tripWhy`.
Points: FI100, LIC101, FIC102, P101, TIC201, TIC202, TIC301, LIC401, PIC401, FIC211, TIC212, TIC213, PI214, LI215, M202, FIC310, TIC311, TI312, FIC313. Cascades: LIC101→FIC102, TIC201→TIC202, TIC212→TIC213.

### 2.5 PID (1182–1203)
Loop order (primaries first): LIC101, TIC201, TIC212, FIC102, TIC202, TIC213, TIC301, LIC401, PIC401, FIC211, FIC310, TIC311, FIC313. `casMap` FIC102:op*1.2, TIC202:10+0.6*op, TIC213:op*1.3 (+ inverse map). INITMAN back-calculation when slave not in CAS. Error in % of span; `Pt=K*e`; `I += K*e*dt/(T1*60)` (T1 minutes); derivative on PV `D = -K*T2*60*dpv/dt`; `op=Pt+I+D` clamped to OPHILM/OPLOLM with integral back-calc. Equation text at template line 514.

### 2.6 Process state `this.P` (942)
`{ t, qin, tankL, flow, conc, rT, Tj, Tcw:8, hxT, drumL, drumP, foulF:1, up:0, trips:{ovf,rx,psv,batch,bed}, faults:{surge,xmtr,cool,rxn,foul,vap,air,pump,agit,stick,bedact}, faultT:{}, b:{phase:'IDLE',pt,Cm,lvl:12,T:25,Tj:20,mf,held}, h:{f:40,pre:320,bed:380,q:10} }`
U1: feed `qin=60+2sin(up/120)+noise(+50 surge)`; valve lag /3; flow `120*FV102.pos*pump*sqrt(tankL/50)` lag /4; tank `+= (qin-flow)*0.0023148*dt`, HIHI trip 98 % (reset <90), cavitation trip <2 %; jacket effectiveness `0.9*sqrt(TV202.pos)*eEff`; exotherm (pre-B4 inline form, now `src/models.js` cstr) `Qr=213700*exp(-1600/(rT+273))*conc*(rxn?1.15:1)`, `rT += (Qr - 30(rT-Tj) - 0.2 flow (rT-40) - 2(rT-25))*dt/20000`, R-201 trip 185 °C (reset <160); fouling to 0.6 over ~10 min; HX `rT + 60*TV301.pos*foulF` lag /90; flash drum vapour fraction, level, `drumP += (vap-vent)*dt/8`, PSV lift 950 kPa; measurement noise; xmtr fault → BADPV after 5 s + shed; pump fault one-shot trip.
U2: monomer inflow lag; jacket `med=15+105*JV213.pos`; kinetics `kk=0.9*exp((T-80)/22)*(M202.run?1:0.18)`, `UA` 1 or 0.3; monomer inventory Cm; energy balance; R-202 trip 110 °C forces COOL.
U3: `preSS=150+420*FV311.pos-0.9(f-40)` lag /22; `bedSS=pre+92*act*exp((bed-380)/150)*(f/40)-3.2q` lag /35; bedact ×1.35; R-310 fuel trip 480 °C (reset <400).

### 2.7 Alarm system
Alarm record (963): `{ id, key:'SRC.COND', t, tag, cond, prio, val, eu, desc, ack:false, active:true, shelved:false, until:0 }`. Event record (955): `{ id, t, type:'ALARM'|'OPERATOR'|'SYSTEM', src, desc, oldV, newV, lvl }`. Message record: `{t, txt}`.
Priorities Urgent/High/Low/Journal (Journal = event only). Scan conditions PVHI, PVHH, PVLO, PVLL, DEVHI; motor TRIP; CMDFAIL ad hoc; trip alarms raised from models with equipment tags TK-101, R-201, V-401, R-202, R-310. Flash: `almSty(a)` 1479–1484 (unacked+active alternate; acked steady; RTN-unacked inverse blink). Horn in tick 982–986 (Urgent double 880 Hz, High 640, Low 460 every 4th tick). Shelve auto-acks + silences + writes message; unshelve re-arms horn if still active.

### 2.8 Drills (1249–1339)
Def: `{id,name,fault,rel:[tags],act,stable:'contain'|'alarms',peak?,needBatch?,when?,q,opts:[4],a}`. Metrics `d.m = {tAlarm,tSil,tAck,tAct,tStable,trip,peak}`. Grades (1762): ≥85 PROFICIENT, ≥65 COMPETENT, else NEEDS PRACTICE.

### 2.9 Instructor (1739–1742)
11 fault checkboxes (xmtr, surge, pump, cool, stick, vap, air, rxn, foul, agit, bedact), speed chips [0,1,2,5], RESET PROCESS → initSim().

### 2.10 Ops Assistant (1135–1181)
Rule ids: trip.rx, trip.b, trip.bed, trip.ovf, trip.psv, mtrip.<motor>, risk.rx (slope helper 1141), risk.bed, risk.acc, badpv.<tag>, stuck.<tag>, sat.<tag>, init.<tag>, flood (≥6 unacked), air, man401, lock.<motor>, fallback ok. Severity URGENT|WARN|INFO. Steps may carry `go` closures (gFp, gDet, gAlm, gTg). Q&A: 14 topics, keyword scoring at 1722, top 3.

### 2.11 Security
`can('OPER')` for ack/shelve/mode/entry/motor/sequence; `can('ENGR')` for tuning, trip points, priority cycling, control action. SUPV and MNGR are never used as gates (help text at 717 claims alarm-disable needs MNGR).

### 2.12 Trends (1561–1578)
TG01 reactor, TG02 feed/tank, TG03 separation, TG04 batch, TG05 fired reactor. Pen tuple `[tag,'pv'|'sp'|'op',color,dash?]`; `mkPens` maps history to polylines; axis constants x=44+…*1006, y=404-…*394.

## 3. Render contract
- `renderVals()` returns one flat object (literal at 1765–1792); keys: showBadge, hcol, menus, tbtns, closeMenus, cmdVal, onCmd, onCmdKey, msgTxt, al, cU, cH, cL, isG1..isG3, isAlarms, isEvents, isMsgs, isTrend, isDetail, isSys, gvList, vlist, gfx, av, evR, evFilters, msgsR, msgsEmpty, tv, dpt, fps, dg, utabs, gv2, vl2, gfx2, batch, gv3, vl3, gfx3, asst, entryText, entryChange, entryKey, entryRef, db, rootMove, rootUp, sysPanels, ledSrv, ledCee, spdT, instrOpen, audT, audCb, secT, secCb, dateT, timeT.
- Handlers are closures created fresh inside renderVals (`setS(o)` factory at 1485); closures capture `S=this.state` at render time.
- Process mutations become visible on the next tick's setState; handlers that only mutate L/alarms rely on the 2 Hz pump.
- `entryRef` is a React ref callback that focuses the entry input.

## 4. Extension points and hazards
- New alarm condition/state: `scan()` 1218–1220, point `alm{}` literals 921–938, `condNote` maps 1616 and 1638, faceplate tick colours 1681, priority cycle 1621/1643, colour maps 956/957, rank maps 989 and 1522.
- New alarm-summary column: header row 334 + row markup 339 + row object 1543–1547 (literal px widths).
- New display: `isXxx` flag at 1770, an `sc-if` block, a `nav()` target, View menu 1490, toolbar 1498–1507, sysLinks 1743, parseCmd map 1357.
- New point: `this.L`; both history tag arrays at 944 and 1066 (must stay in sync); `gpts` 1516–1519 or `mkGv` lists 1533/1534; `unitOf` 1129–1131; PID loop order 1186 + casMap/invMap.
- New unit: utabs 1708, isG4, sc-if SVG block, stepU4 from step(), unitOf, TGS group, peakOf keys, gfx helpers.
- New drill: drillDefs, dAct verb, injectFault, endDrill clear list, instructor checkbox.
- Global side effects: setInterval 500 ms (910), keydown listener (912, F1–F4 preventDefault), lazy AudioContext (972), `entryRef` focus. No localStorage, fetch, or persistence; reset = initSim().
- Fragile: `l.alm[cond]` is a bare `[tripPoint, priority]` tuple mutated by index (1216, 1428, 1621, 1643, 1553, 1682); history rows are 4-tuples with positional index map at 1569; `this.events[0].oldV=` mutation right after addEvent (1407, 1424, 1436, 1437); `eq2()` hoisted function declaration at 1706 used at 1692 (do not convert to const); string-typed enums everywhere; tick 500 ms coupled to `step(0.5)` (speed multiplies iterations, does not scale dt); magic numbers (viewBoxes 1420x720, trend 1060x430, trip thresholds 98/185/950/110/480 repeated in assistant prose 1142–1149 and help 1179); caps events 600, hist 7200, event view 220, messages 120, issues 8; `state.fps` object identity coupling (1686–1688 vs 1777); `this.state.msg=''` direct mutation at 979; `initSim()` called lazily from 909, 976, 1474; dead code at 1757; `pump`/`agit` faults are one-shot (1279–1280); `foul`/`air` not in endDrill cleanup.

## 5. Size
Template 895 lines (~52 KB; unit SVGs 251 lines, dialogs 130). Script 891 lines; `renderVals()` is 321 lines (36 %); models 131 lines; assistant 47 lines. No module boundaries; comment banners at 948 helpers, 974 simulation, 1248 drills, 1341 operator actions, 1472 render are the natural seams.

## v2 changes: B1-alarm-engine (2026-08-29)

- **New scripts in the head** (before `support.js`, inlined into the standalone by `tools/build-dist.py`): `src/alarm-engine.js`, `src/pid.js`, `src/kpi.js`, `src/palette.js`, `src/models.js`. Only the alarm engine is used by the app in this step; the others are loaded for B3/B4.
- **Engine change**: `engine.oos(ref, t, spec)` accepts a `spec` (`{tag,cond,prio,subprio,val,eu,desc,tripValue}`) and creates the record directly in OOSRV when none exists, so a condition that has never alarmed can be taken out of service.
- **Instance fields**: `this.alarmEngine` (ISA-18.2 engine, `maxShelveMs` 60 min, `defaultShelveMs` 15 min, `foldWindowMs` 10 min, DAS rules from `dasRules()`). `this.alarms` is now a **getter** returning `alarmEngine.list()` (every record not in NORM; read-only, do not assign). `this.aid` removed. Per point: `l._am[cond]` limit memo `{raw,active,onT,offT}` (engine `evaluateLimit`), `l._as[cond]` stays the boolean "condition active" flag, optional `l.almDb` (deadband in EU, default 1 % of span) and `l.almDelay` (on-delay s; defaults: tag starting `F` 15 s, `L` 60 s, else 0).
- **Point config**: `l.alm[COND]` is now `[tripPoint, priority, subprio]`; the third element is filled at `initSim` from `subprioDefault(cond)` (TRIP 15, PVHH/PVLL 12, PVHI/PVLO 8, DEVHI 6, CMDFAIL 4, BADPV 10, trip-class equipment alarms 15).
- **Alarm record** (engine shape): `{id,key,tag,cond,prio,subprio,state,t,lastT,rtnT,ackT,count,val,eu,desc,tripValue,ack,active,shelved,shelveReason,until,comment,suppressedBy}`; `state` in NORM/UNACK/ACKED/RTNUN/SHLVD/DSUPR/OOSRV. Use `lastT` for the displayed time (folded repeats), `t` is first occurrence.
- **Methods** (thin wrappers, same names for drills / assistant / command zone): `raiseA(src,cond,prio,val,eu,desc)`, `clearA(src,cond,val?)`, `ackAlarm(refOrRecord)`, `ackTop`, `ackPage`, `visAlarms()` (view-filtered, sorted with `engine.compare`), `shelve(mins)` (reason mandatory, 1–60 min), `unshelve()`, `topUnack()`, `shelfScan()` = `alarmTick()` (engine tick: shelve expiry, NORM pruning). New: `hornTop()` (UNACK only), `logAlarmEvents(descriptors)` (journals ALARM / RTN / ACK / SHELVE / UNSHELVE / SUPPRESS / UNSUPPRESS / OOS / RTS through `addEvent` and re-arms the horn when anything lands in UNACK), `subprioDefault`, `subprioOf`, `tripPointOf`, `dasRules()`, `almDeadband(l)`, `almDelay(l)`, `scan(dt)` (dt defaults to 0.5), `isOos(tag,cond)`, `setOos(tag,cond,on)` (ENGR+), `shelveReasons()`, `shelveReasonText()`.
- **Journal**: every alarm produces `COND ALARM PRIO SUBPRIO` on entry and `COND RETURN TO NORMAL` on return (type ALARM); operator ACK / SHELVE / UNSHELVE / OOS / RTS are OPERATOR events with from/to states in old/new; DAS and auto-unshelve are SYSTEM events. No RTN event is journaled for an OOSRV record.
- **Indication**: `almSty(a)` uses `engine.indication(state, prio)`: UNACK blinks, RTNUN inverse-blinks, ACKED steady, SHLVD/DSUPR/OOSRV grey. Horn only for UNACK (`hornTop`). Counters `cU/cH/cL` = `engine.counts()` priority buckets, which cover UNACK+ACKED only; `counts().unack` (status bar `UNACK n`) is UNACK+RTNUN, so a returned-but-unacknowledged alarm stays in the unack count without inflating the active counters (fixed in verification round 1).
- **State**: `shelfView` now also `SUPPRESSED` | `OOS`. Shelve dialog is opened only through `openShelve()` (checks a selection, sets `dlg:{type:'shelve',dur:15}` so the default duration chip is visible, clears `dlgReason`); `shelve(mins)` refuses with `SHELVE REASON REQUIRED` / `SHELVE DURATION REQUIRED` and clamps 1..60. `shelveDurations()` returns the chip list.
- **renderVals keys added**: `cUn`, `cUnFg`, `cOos` (status bar); `av.rows[].state`, `av.rows[].prio` is `'HIGH 8'` style; `av.filters` has five chips; `dg.reasons[].{label,bg,bc,cb}`, `dg.durs` now 5/15/30/60; `dpt.almRows[].{oosT,oosBg,oosCb}` and `dpt.almNote` (deadband / delay text); alarm line `al.txt` includes priority, sub-priority and state.
- **Template**: Alarm Summary header/row gained a 62 px State column (Priority widened to 82 px); Point Detail ALARMS tab gained a Service column with an OOS/RTS button; shelve dialog has reason chips + detail input; status bar shows `UNACK n` and `OOS n`; help dialog lists the states.
- **DAS map** (`dasRules()`): P101.TRIP → FIC102 PVLO/PVLL, LIC101 PVHI/PVHH; TK-101 HIHI TRIP → LIC101 PVHI/PVHH; R-201 HI TEMP TRIP → TIC201 DEVHI, TIC202 DEVHI/PVHI, FIC102 PVLO/PVLL; R-202 HI TEMP TRIP → TIC212 PVHI/PVHH; M202 TRIP → TIC213 PVHI; R-310 HI TEMP TRIP → TIC311 PVLO. Suppressed alarms are listed under the SUPPRESSED filter with `[SUPPRESSED BY trigger]`. **Deliberate deviation from the plan's examples**: `TIC201 DEVHI [15 DEG C, High]` was added so the R-201 entry can be honoured; `FIC211 PVLO` was NOT added because the monomer feed idles at zero flow outside the FEED phase and a fixed PVLO would be a standing alarm, so M202 TRIP suppresses the jacket consequence TIC213 PVHI instead until state-based limits arrive in B4. `tests/app-alarms.test.js` asserts every key in the map is a configured condition.
- **Tests**: `tests/app-alarms.test.js` (14 app-level cases) and engine cases for `oos(spec)` and RTNUN counting. Run with `node --test tests/*.test.js` from the repo root (UPGRADE-PLAN rule 5; newer node 22 builds reject a bare `tests/` directory argument).
- Hazards: `hold()`-style tests must advance `P.t` themselves; `scan()` reads `l._as` for the assistant's saturation rule, keep it in sync; drill `tAlarm` now uses `lastT`.

## v2 changes: B2-alarm-summary (2026-08-29)

- **New file** `src/alarm-help.js` (`ESS.AlarmHelp`, loaded in the head after `models.js`, inlined by `tools/build-dist.py`): the rationalisation table for every alarm condition in the sim (46 point conditions + P101/M202 TRIP, P101 CMDFAIL, FIC102 BADPV, the five equipment trips and V-401 PSV LIFT). `resolve(tag,cond,cfg)` returns `{found,priority,setting,responseTime,consequence,probableCause,correctiveAction}`; priority and setting are built from the live point config (`cfg`) so the help can never disagree with the trip point. `EQUIPMENT_TRIPS` carries the model thresholds (98 % / 185 / 950 / 110 / 480) and is the fallback for `tripPointOf()` on equipment sources. Fields per PAS/Hollifield (RESOURCES 2.10); pane concept per the Experion Alarming PIN (2.2).
- **New file** `tests/app-alarm-summary.test.js` (7 cases: asset filter + counts + ACK PAGE scope, trip/live columns, repeat folding + chatter, comment, saved views, KPI flood, alarm help for every condition incl. Point Detail and pane rendering).
- **State added**: `almAsset` ('PLANT' | 'U1'|'U2'|'U3' | equipment id; Alarm Summary location filter), `almHelp` (bool, help pane), `savedViews` (`[{name,asset,view}]`), `detailAlmCond` (condition shown in Point Detail > Alarms help), `kpiSpan` (10|30|60|0 minutes, 0 = session). `display` gains `'kpi'`. Dialog type `'comment'` reuses `dlgReason` for the text.
- **Instance fields**: `this.alarmLog` (`[{t,key,tag,cond,prio,type:'raise'|'rtn'|'ack'}]`, appended in `logAlarmEvents`, cap 5000) and `this.t0` (session start ms). Both reset in `initSim`.
- **Methods added**: `alarmHelpFor(tag,cond)`, `helpFields(h)`, `assetTree()`, `assetOf(tag)`, `assetMatch(nodeId,tag)`, `assetCounts()` (active = UNACK+ACKED, unack = UNACK+RTNUN per node), `liveValueOf(a)` / `liveDec(a)` (point PV, PV−SP for DEVHI, model variable for equipment sources), `builtinViews()`, `savedViewName()`, `saveView()`, `applyView(v)`, `openComment()`, `commentAlarm(text)` (OPER+, engine `comment()`, OPERATOR event `COND COMMENT: text` with old/new, message), `kpiSpans()`, `kpiMetrics(span)` (`ESS.Kpi.computeMetrics` over `alarmLog`, stale threshold 10 min for the training scale), `trackerLanes(mins)`, `kpiView(setS)` (KPI display view model).
- **Changed contracts**: `visAlarms()` now also filters by `state.almAsset`, so `ackPage()` acknowledges only the visible rows. `tripPointOf()` falls back to `EQUIPMENT_TRIPS`. `parseCmd` accepts `KPI` / `ALMKPI` / `ALARM KPI` and `ALMHELP` (toggles the pane and opens the summary).
- **renderVals keys**: new top-level `isKpi`, `kpi` (`verdict,verdictBg,verdictFg,windowTxt,spans[],tiles[].{label,value,sub,fg},checks[].{label,value,limit,res,fg},prio[].{label,w,color,txt},prioNote,lists[].{k,v},actors[].{rank,key,count,pct,w,cb},actorsEmpty,footer`). `av` gains `tree[].{label,pad,glyph,fw,active,unack,actFg,unFg,bg,fg,cb}`, `views[].{label,note,bg,cb}`, `tracker[].{label,bars[].{x,y,w,h,fill,cb},cb}`, `trackerSpan`, `trackerNow`, `helpOn/helpBg/helpBc/helpToggle`, `help.{title,fields[].{k,v},comment}`; `av.rows[]` gains `trip`, `live`, `cnt`, `cmt` (`val` removed; header and row markup changed together); `av.btns` adds COMMENT… and SAVE VIEW. `dpt` gains `helpConds[].{label,bg,bc,cb}` and `help.fields[]`. `dg` gains `isComment`, `commentCur`, `commentGo`, `commentKey`.
- **Template**: Alarm Summary is now three panes (196 px location tree + saved views, scrolling list with columns icon 20 / Time 66 / Source 66 / Condition 84 / Priority 72 / State 54 / Description flex ≥150 / Trip 62 / Live 62 / Units 48 / Cnt 30 / C 22 / Ack 34 inside an `overflow-x:auto` column with 820 px minimum, Alarm Tracker strip of three unit lanes over the last 30 min with 1-min bars, optional 300 px Alarm Help pane). Point Detail > Alarms has an ALARM HELP block with a condition chip row. New `isKpi` display (span chips, verdict badge, six tiles, health checks table, priority bars vs 80/15/5, floods/chattering/standing, top-10 bad actors clickable to Point Detail, footer with thresholds and citations). New comment dialog. View menu, toolbar (`KPI`) and sysmenu link added; help dialog mentions the new controls.
- **Hazards**: `kpiMetrics(span>0)` windows on real elapsed time even when the session is younger than the span (the window text says when the session started); the SESSION span starts at `this.t0`. The tracker strip is `position:sticky;left:0` so it stays visible when the list is scrolled horizontally. `assetTree()` is rebuilt on every call (cheap, 10 nodes); `assetOf` covers alarm sources only, `unitOf` remains the authority for unit membership of nodes without a `tags` list.

## v2 changes: B3-colour-limits (2026-08-29)

- **New file** `src/philosophy.js` (`ESS.Philosophy`, loaded in the head after `alarm-help.js`, inlined by `tools/build-dist.py`): our own prose for the "Alarm and display philosophy" help page — `sections(ctx)` (grey backgrounds, priority meaning and the 5/10/85 vs 80/15/5 distributions, ISA-18.2 states and blinking, shelving vs suppression vs out-of-service, deadband and on-delay, the limit ladder), `sources()` (ISA-18.2, EEMUA 191, ASM Consortium gray-background paper, Rockwell process HMI style guide, cited by name only) and `limitLadder()` (the eight rungs). `src/palette.js` presets gained a `band:{target,normal,range,marker}` group for the limit band fills.
- **New file** `tests/app-palette-limits.test.js` (7 cases: palette switch changes `av.rows`, the alarm line, counter chips, graphic alarm box and faceplate ticks; data-prop + highColor feed the getter; every priority/text pair ≥ 3:1 in both presets; ladder ordering for every point in `this.L` plus band rendering on overview / faceplate / Point Detail; ENGR target-band edits with rejection rules; philosophy dialog cites the four sources; Loop Tune ISA form, PV tracking and sparkline).
- **Colour getter**: `paletteName()` (state `palette` → data-prop `palette` → `'representative'`), `colours()` (memoised `ESS.Palette.getPalette`; the `highColor` data-prop still overrides High on the representative preset only), `prioColor(p)`, `prioDark(p)`, `prioText(p)`, `paletteLabel(n)`, `setPalette(name)` (SYSTEM event `ALARM COLOUR PHILOSOPHY CHANGE`, message zone). Every priority literal in `renderVals`/`kpiView` now goes through these (`almSty`, counter chips, tracker bars, faceplate ticks by the condition's configured priority, motor TRIP badge, batch TRIP pill, assistant severity, KPI verdict/priority bars). Chrome and equipment stay grey (ASM, RESOURCES 2.11); the palette `bg`/`line` values are not applied to the chrome.
- **Data-props**: new `palette` enum (`representative` | `isa101`) beside `highColor`. **State**: `palette` (null = follow the data-prop). Dialog type `'philosophy'`.
- **Point config**: PID factory adds `tgtLo:null, tgtHi:null` (null = auto target band = SP ± 5 % of range) and `pvtrack:false`; indication points carry a configured normal band (FI100 50–70, PI214 80–320, LI215 10–80, TI312 360–420).
- **Limit ladder methods**: `limitBand(l)` → `{rangeLo,critLo,stdLo,tgtLo,tgtHi,stdHi,critHi,rangeHi,auto}` (range = lo..hi, critical = PVLL/PVHH, standard = PVLO/PVHI, target = engineer band or SP ± 5 %; each rung clamped inside the outer one so the ladder is always ordered; missing rungs collapse outward); `bandSegments(l,h)` → `{segs[].{y,h,fill},marker,markerFill,band}` (critical/standard zones take the guarding alarm's priority colour, the rest `palette.band`); `setTargetBand(l,lo,hi)` (must be inside the standard band, lo < hi); `setPvTrack(tag,on)` (ENGR, OPERATOR event `PV TRACKING CHANGE`). `pids()` makes SP track PV in MAN when `l.pvtrack` (clamped to SPLOLM/SPHILM).
- **Entry params**: `TGTHI` / `TGTLO` (ENGR; `openEntry` reads the live band, `commitEntry` routes through `setTargetBand`; events `TARGET HIGH CHANGE` / `TARGET LOW CHANGE`).
- **renderVals keys**: `cnts[].{label,n,bg,fg,bd,title}` (counter chips; `hcol` kept), `gvList/gv2/gv3[].{bandOn,bx,band[].{y,h,fill},bmx,bmy,bmFill}`, `fps[].bands[]`, `dpt.{hasBand,limitRows[],band[],bandMarker,bandMarkerFill,bandNote,spark.{span,pv,sp,op,note}}`, `dpt.tuneRows` adds `ISA`, `PARALLEL`, `PVTRACK` rows (values from `ESS.Pid.isaForm`), `dg.isPhil`, `dg.phil.{sections[],sources[],ladder[],palettes[],swatches[]}`. New methods `philosophyView()`, `stepSparkline(l,mins)` (PV/SP/OP over the last 5 min on a 320×90 box, last SP step noted).
- **Template**: counter chips are an `sc-for`; each overview point box has a 5 px band to its left with a PV marker (motors excluded); PID and indication faceplate bars draw the bands behind a narrower PV fill; Point Detail > Main has a LIMIT LADDER block (160 px band + 8 rows, target rows editable); Loop Tune rows honour `cur/und`, plus ISA-form text and a step-response sparkline; new philosophy dialog (680 px). Station menu: two "Alarm colour philosophy" items; Help menu: "Alarm and display philosophy…"; command zone `PHILOSOPHY` / `ALMPHIL`.
- **Hazards**: `colours()` memoises on palette name + highColor; add any new palette-derived field to `ESS.Palette` rather than the app. The band opacity (0.6 SVG / 0.55 faceplate / 0.7 detail) is what keeps the priority colours quiet on the grey ground; do not raise it. `openFp` still replaces unpinned faceplates, so tests open one at a time.

## v2 changes: B4-process-models (2026-08-29)

- **Models wired in**: `initSim` builds `this.P` with `ESS.Models.createState(now)`; `step(dt)` is now `advanceClock` → `ESS.Models.stepU1` → `stepU2()` → `stepU3()` → `pids` → `scan` → `interlocks()` → `alarmTick` → `drillWatch` → history. `stepU2(dt)` / `stepU3(dt)` keep their names but are thin wrappers: `ESS.Models.stepU2/3` plus the station-side indications (`TI216.pv = P.b.Tad`, `TI314/TI315.pv = P.h.ts1/ts2`, `AI316.pv = P.h.o2`; `AI205.pv = P.x*100` is written in `step`). The inline U1/U2/U3 equations are gone (CODE-MAP 2.6 now describes `src/models.js`). `modelCtx()` is the memoised callback bundle (`raise/clear/tripMotor/addEvent/rand/shed/message/onTrip`), `noise(k)` the measurement noise helper, `histTags()` the single history tag list. New instance fields: `this.rand` (null = `Math.random`; tests set `ESS.Models.createRand(seed)` for determinism), `this.callouts`, `this.phaseSet`, `this.tadShed`, memo `this._ctx`, `this._pidCtx`.
- **Module changes**: `src/models.js` — `moveValves` closes FV311 for `P.trips.skin` as well as `P.trips.bed`; the `vap` fault multiplier is 2.6 (was 1.9: PIC401 left in MAN never reached PVHI, so drill D9 could not annunciate; in AUTO the vent still holds the drum below 750 kPa). `src/pid.js` — PV tracking is skipped while `badPv` (the shed already holds the loop). `src/alarm-help.js` — entries for FIC211.PVLO, LI215.PVLO, TI216.PVHI/PVHH, TI314/TI315.PVHI/PVHH, AI316.PVLO and the equipment trip `H-310.TUBE SKIN TRIP` (500 DEG C) in `EQUIPMENT_TRIPS`.
- **PID**: `pids(dt)` runs `ESS.Pid.stepPid` per loop in `pidOrder()` with `pidCtx()` (same cascade maps as before, now shared). No retune was needed: the baseline, every drill and the batch pass with the previous K/T1/T2. `setMode` uses `ESS.Pid.transferMode` (bumpless: integrator re-initialised so the first AUTO/CAS output equals the current OP; refuses CAS without a master and non-MAN with a bad PV). `pvtrack` defaults **on** for the cascade secondaries FIC102, TIC202, TIC213 (`ENGR` toggle on Loop Tune unchanged).
- **New points** (`this.L`, history, `unitOf`, asset tree, graphics, trends): `AI205` R-201 conversion (ind, %, no alarms, U1 box at 684,270); `TI216` R-202 adiabatic end temp (ind, PVHI 100 High / PVHH 106 Urgent, U2 box at 790,506, pen on TG04); `TI314` / `TI315` H-310 pass 1 / pass 2 tube skin (ind, PVHI 440/450 High, PVHH 490/500 Urgent, U3 boxes at 40,470 / 40,530 with dashed leaders to the tube passes, pens on TG05); `AI316` flue-gas excess O2 (ind, PVLO 1.5 Low, `almDelay` 10 s, U3 box at 300,170 beside a new stack glyph). FIC211 carries `almDelay: 30`. System Status reports 24 CMs.
- **Interlocks** (`interlocks()`, after `scan` so deadband / on-delay apply): TI216 PVHH is **level-held** (verification round 1): `latchTadShed()` sets `tadShed`, forces FIC211 to MAN / OP 0 (MV-211 closed), sets `P.b.held` when in FEED and journals a SYSTEM event on TI216 whose `· SCM202 HOLD` clause and `SEQUENCE HELD` message appear only when a hold actually happened; `enforceTadShed()` re-asserts MAN / OP 0 / SP 0 (and the hold in FEED) on every scan while the condition stands; `interlockOwns(tag,param)` makes `operatorMayWrite` refuse FIC211 MODE / OP / SP with a `TI216 URGENT INTERLOCK` callout, and while `tadShed` holds the sequence the U2 button reads HOLD (not RESUME; `batch.holdT`) and `seqCmd('HOLD')` runs `confirmInterlockHold()` — message `SEQUENCE ALREADY HELD — TI216 INTERLOCK`, OPERATOR event `HOLD CONFIRMED — SEQUENCE ALREADY HELD BY TI216 INTERLOCK`, `dAct('HOLD','SCM202')` so drill D11 credits the operator's hold order after the latch (round 2; RESUME is never offered, so it is never refused); `releaseTadShed()` journals `… SHED RELEASED, RESUME PERMITTED` when PVHH clears (the sequence stays held until the operator resumes). `scmRestoreModes()` (called from `stepU2` after the model's SCM step) returns FIC211 to AUTO through `ESS.Pid.transferMode` with a SYSTEM event `FIC211 MODE RESTORED BY SEQUENCE (MAN → AUTO)` whenever the loop is PROGRAM-owned but not in AUTO (RESUME after a shed, FEED entered with the loop left in MAN), unless the PV is bad or the interlock stands — so a shed batch is recoverable without ABORT. Assistant `shed.tad` / `mtrip.M202` / `risk.acc` steps direct the operator to SCM202 HOLD on the U2 graphic (`gSeq` closure) because FIC211 refuses MAN / OP stores under PROGRAM. TI314/TI315 PVHH sets `P.trips.skin`, raises `H-310 TUBE SKIN TRIP` (Urgent) and counts as a drill trip; resets when both skins are below 400 DEG C (`stepU3`; the assistant and Alarm Help quote the same 400). `liveValueOf` returns the hotter skin for that record.
- **State-based alarm limits** (ISA-TR18.2.6 pattern, RESOURCES 2.19): `phaseSets()` is the per-phase table for TIC212, TIC213, LI215, FIC211, TI216 over IDLE / CHARGE / HEATUP / FEED / HELD / REACT / COOL / DRAIN (`HELD` = FEED with the sequence held). `phaseSetKey()`, `phaseSetTags()`, `syncPhaseSet()` (called from `stepU2`, `seqCmd` and the TI216 interlock) and `applyPhaseSet(key, silent)`: an entry `[tp, prio]` rewrites `l.alm[cond]` (sub-priority kept), `null` moves the tuple to `l.almOff[cond]`, clears any active alarm and drops the limit memo. Every change journals `SCM202 ALARM LIMIT SET <KEY> — TAG COND value P · …` (old/new = previous/new key); `initSim` applies IDLE silently. Highlights: LI215 PVLO off in IDLE/CHARGE/DRAIN (30 in HEATUP/FEED, 60 in REACT/COOL); FIC211 PVLO 8 only in FEED, PVHI 28 in FEED and 8 elsewhere; TIC212 PVHI/PVHH 95/105 base, 90/102 FEED, 88/100 REACT; TIC213 PVHI 100 in FEED/REACT; TI216 98/105 in REACT. ENGR trip-point edits on these points last until the next phase change. Point Detail > Alarms: `dpt.almNote` carries `Phase set: <KEY>`, disabled conditions render as rows with state `DISABLED` (`mkOffRow`, from `l.almOff`).
- **PROGRAM mode attribute**: `operatorMayWrite(tag, param)` (via `ESS.Pid.canOperatorWrite`) gates `openEntry` SP/OP, `commitEntry` SP/OP, `setMode` and `raiseLower`; a refusal calls `rejectWrite(tag, txt)` → `this.callouts[tag] = {txt, until: P.t + 5000, untilTk: state.tk + 10}` (expires on sim time or after ten UI ticks, so a paused sim still clears it; round 2), OPERATOR event `WRITE REJECTED — MODE ATTRIBUTE PROGRAM — <PARAM> OWNED BY SEQUENCE`, message zone. `calloutOf(tag)` returns the live text (expires itself). Engineering parameters stay writable. The attribute is still set by the model's SCM (TIC212 for the whole batch, FIC211 during FEED unless held) and returns to OPERATOR when the sequence ends.
- **renderVals keys**: `fps[].{calloutOn, calloutT, frameBc}` (red frame + callout box under the faceplate note), `dpt.{calloutOn, calloutT}` (box under the MODE row) and the SP / OP `mainRows[].rowBg` tint while a callout is live; `dpt.almRows[]` may contain DISABLED rows; `gvList` / `gv2` / `gv3` carry the new boxes; TG04/TG05 have five and six pens.
- **Drills**: drill defs accept an optional `setup(component)` hook run by `startDrill` after the batch pre-start; D9 uses it to leave PIC401 in MAN (SYSTEM event `MODE LEFT IN MAN BY PREVIOUS SHIFT (DRILL SETUP)`), which is the condition its quiz already assumed.
- **Assistant**: rules `trip.skin`, `shed.tad`, `risk.tad`, `risk.skin`, `o2.low`; topics for PROGRAM write rejections, PV tracking / bumpless transfer and state-based limits.
- **Tests**: `tests/app-models.test.js` (17 cases: 30-min baseline through `step()`, every drill's first alarm inside 12 min, a full batch with the limit-set journal, phase-set mechanics + Point Detail, PROGRAM rejections with callouts and expiry, PV-tracking bumpless transfer on LIC401 vs. no tracking, TI216 shed + hold, tube-skin trip + O2 low + graphics/trend presence; round 1 added: level-held interlock refuses AUTO / OP and the feed cannot restart, RESUME after the shed clears lets the SCM restore FIC211 to AUTO and the batch completes, TI216 Urgent outside FEED sheds without a HOLD, the 400 DEG C skin-trip reset agrees across model / assistant / help, M202-trip advice points to HOLD with a GO to the U2 graphic; round 2 added: D2 correct action ends with no R-201 / TK-101 trip, D6 TIC202-to-MAN alone stays under the trip and stabilises, D11 HOLD after the interlock latch is credited, callouts clear after ten ticks while paused). `tests/models.test.js`: the stick case asserts DEVHI + TIC201 PVHI without a trip, the surge case asserts the tank absorbs the surge (LIC101 PVHI, SP capped at 80, no trip).
- **Round-2 calibration** (`src/models.js`, `this.L`): feed concentration is fixed (Henson/Seborg `Caf`; `P.conc` is now an indication of flow/design only) — throughput acts through residence time, giving R-201 about +33 % feed margin; `FIC102.sphilm` is **80** (was 110) so the LIC101 cascade cannot run the reactor into its trip to empty the tank — a +50 m3/h surge now peaks TK-101 near 86 % (LIC101 PVHI, no HIHI) with R-201 near 174 (TIC201 PVHI, no trip); the `stick` fault no longer sets `faults.rxn` — its reaction load is `PARAMS.U1.stickHeat` (1.15 on heat only) so TIC202-to-MAN keeps R-201 near 162; `PARAMS.U1.surge` (50) is the surge magnitude. No PID retune. Known and unchanged: D4's CUTFEED (FIC102 OP 20) still lets TK-101 reach its HIHI trip after ~9 min if the operator does not restore feed (same in the pre-B4 build). `tests/models.test.js` still drives the module with its own rig and stays green; the IDLE/CHARGE/DRAIN TIC212 limits stay at 95/105 so that rig (which never applies phase sets) sees no nuisance alarm.
- **Hazards**: loops with a derivative term (TIC201/TIC301/TIC311/TIC212) jitter several % per step on measurement noise in every mode — judge bumpless transfer on a P+I loop. `phaseSets()` is rebuilt per call (cheap); the `HELD` key must stay in sync with `phaseSetKey()`. `tadShed` and `P.trips.skin` are not cleared by `endDrill`; `initSim` resets both. The skin trip re-opens FV311 when it resets, so TIC311 left in MAN at a high output will cycle the trip — the same behaviour as the bed trip.

## v2 changes: B5-instructor (2026-08-29)

- **New file** `src/instructor.js` (`ESS.Instructor`, loaded in the head after `philosophy.js`, inlined by `tools/build-dist.py`): instructor-station data and helpers — `create()` / `resetRun()`, `makeSnapshot()`, the 30 s / 10 min backtrack ring (`pushRing`, `ringPick`, `trimAfter`), the action journal (`journalAdd`, `replayPlan`, `replayDue`, `journalText`), `presets()` (five initial conditions as plain data), `upsetDefs()` (12 upsets incl. the new `drift`, with magnitude controls), `variableDefs()` (six instructor variables with `path` into `P`), `speeds()` = 1/2/5/10, constants `RING_MS`, `RING_SPAN_MS`, `SLOTS` (8), `DEFAULT_SEED`. Sources: Forge PTS instructor feature list (RESOURCES 2.14), cstr-ots seeded determinism / journal replay / snapshot-backtrack notes (RESOURCES 4).
- **New file** `tests/app-instructor.test.js` (10 cases: snapshot round trip with deep equality of P / L / V, alarm records and sim time; ring buffer 21 entries at 30 s spanning 10 min plus backtrack buttons; freeze / STEP / RUN / 10×; journal replay reproduces PV trajectories, operator events and the journal itself; seed determinism; hidden upset leaves no trace in any trainee display and the assistant still infers the air loss; OPER / SUPV refused, `instr` / `mngr` open, LOCK, command `INSTR`, status-bar link; presets reach their condition; magnitudes / variables / drift; live assessment).
- **Module changes**: `src/models.js` — `createRand(seed)` exposes `fn.seed`, `getState()`, `setState()`; `createState` adds `env` (`feedConc, Tamb, foulRate, catAct, monoPurity`), `mag` (`surge, coolLoss, bedact, drift`) and `driftOff`; the models read them (`Caf *= env.feedConc`, ambient from `env.Tamb` in U1 and U3, fouling × `env.foulRate`, reactive monomer × `env.monoPurity`, bed activity `env.catAct × (bedact ? mag.bedact : 1)`, surge `mag.surge`, cooling outage `1 − mag.coolLoss`, new `drift` fault walking LIC101 by `mag.drift` %/min); `envDefaults()` / `magDefaults()` exported. `src/alarm-engine.js` — `engine.snapshot()` / `engine.restore(data)` (records + id counter as plain data). `src/alarm-help.js` — probable-cause prose no longer names instructor faults.
- **Instance fields**: `this.instr` (survives `initSim`; `{auth, hidden, seed, snapshots[8], ring, journal, replay, log, lastRingT}`), `this.seed`, `this.rand` is now always the seeded generator (`initSim` seeds from `instr.seed`; tests may still replace it), `this.vLag` (valve id → seconds not following its target, fed by `valveWatch(dt)` in `step`).
- **State added**: `instrSnapName`, `instrSeedTxt`; `display` gains `'instr'`; `speed` gains 10; dialog `logon` accepts `instr:true` (instructor logon). Dialog type `'instr'` is gone (the panel became a display).
- **Methods added**: gate `instructorAllowed()` (MNGR or `instr.auth`), `openInstructor()`, `lockInstructor()`, `effDisplay()` (instr falls back to the graphic when not allowed), `instrLog()` (instructor log; mirrored as SYSTEM/INSTR events only when not hidden), `replaying()`; run control `setSpeed`, `freeze`, `stepOnce`, `setSeed`; snapshots `snapshotData(name)`, `restoreSnapshot(snap, why)` (clones P/L/V, restores the engine, interlock flags, seed + generator cursor; truncates history / events / messages / alarmLog / ring / journal after the snapshot time; clears drill, entry, selection), `saveSlot`, `restoreSlot`, `clearSlot`; backtrack `backtrackTick()` (from `step`), `backtrack(ms)`; `applyPreset(id)` (fresh process + data overrides, run to the condition, then `restoreSnapshot`); upsets `upsetOn`, `setUpset`, `clearUpsets`, `setHidden`, `setMagnitude`, `setVariable`, `variableValue`; journal `journal(op, tag, arg, extra)`, `startReplay(slot)`, `stopReplay`, `replayToEnd`, `applyReplayDue()` (start of `step`), `replayCheckDone()` (end of `step`), `applyJournalEntry(e)`; `drillRows(d)` (shared by debrief and assessment); `instructorView()`; valve symptom helpers `valveMap`, `valveTarget`, `valveWatch`, `laggingValves`.
- **Changed contracts**: `can()` returns true while a replayed journal entry is being applied (the journaled actions already passed the gate; see round 1 below); `commitEntry()` parses and delegates to new `storeEntry(tag, param, v)` (returns true when the entry field should close); `ackPage()` delegates to `ackKeys(keys)` (journaled with the keys so a replay acks the same alarms); `shelve(mins)` validates and delegates to `shelveAlarm(key, mins, reason)`; `unshelve()` delegates to `unshelveAlarm(key)`; `seqCmd` journals operator commands only (not `silent`); `startDrill` takes its injection delay from the seeded generator; `logon()` accepts `instr` (sets `instr.auth`, leaves the security level alone) and, in the instructor logon, `mngr`. Journal ops: MODE, STORE (param), RAISE, LOWER, START, STOP, SEQ, ACK (key), ACKPAGE (keys), SIL, SHELVE (key, mins, reason), UNSHELVE.
- **Assistant**: the `stuck.<tag>` and `air` rules are symptom-based (`vLag` ≥ 20 s on one valve; ≥ 3 valves lagging ≥ 10 s = air loss suspected) instead of reading `v.stuck` / `P.faults.air`, so a hidden injection is diagnosed from process data only; the BADPV step and the drills topic no longer use injection vocabulary.
- **renderVals keys**: `isInstr`, `instr` (`{on:false}` for anyone who is not the instructor; otherwise `authT, hidden{label,bg,bc,cb}, hiddenNote, lock, close, seedT, seedVal, seedChange, seedKey, seedGo, run{stateT,simT,upT,freeze,stepOne,runBtn,speeds[]}, slots[].{n,name,t,has,save,restore,replay,clear}, snapName, snapNameChange, presets[].{label,desc,cb}, back{count,spanT,oldestT,btns[]}, upsets[].{k,label,unit,on,stateT,hasMag,magLabel,magVal,magT,magMin,magMax,magStep,cb,magChange}, vars[].{k,label,valT,val,min,max,step,change,reset}, journal{count,empty,rows[].txt,replayOn,replayT,stop,toEnd,clear,note}, assess{on,name,tm,phase,rows[],end,stats}, log[].{t,txt}, logEmpty`); `dg.logonNote`; `spdT` reads `FROZEN` at speed 0; `instrOpen` now calls `openInstructor()`; `sysPanels` SIMULATION ENGINE shows `FROZEN` and, only while upsets are not hidden, an `Upsets` row. Removed: `dg.faults`, `dg.speeds`, `dg.resetGo`, `dg.isInstr`.
- **Template**: new `isInstr` display (header with auth chip, HIDDEN UPSETS toggle, TRAINEE VIEW and LOCK; cards for Run control, Backtrack, Snapshots, Initial conditions, Upsets with range sliders, Instructor variables, Action journal with replay controls, Assessment, Instructor log); the instructor dialog block is removed; logon dialog shows `dg.logonNote`; help dialog documents the station. Drills menu item is now "Instructor Station…"; command zone `INSTR` / `INSTRUCTOR`.
- **Verification round 1 (2026-08-29)**: the journal is sequence-stamped (`instr.seq`, `entry.seq`); a snapshot records `journalSeq` and `restoreSnapshot` / `replayPlan` cut the journal by sequence, so actions taken while frozen at the snapshot's sim time replay and actions journaled before the save are neither replayed nor dropped (time is the fallback for snapshots without a sequence). Instructor interventions are journaled too, flagged `instr:true` — ops `UPSET` (tag = fault key, arg ON/OFF), `MAG` (tag = magnitude key), `VAR` (tag = variable key), `SEED` — and `applyJournalEntry` re-applies them, so a replay reproduces upsets injected after the snapshot. `instrNote(txt)` writes the instructor log only; variables, magnitudes and the seed go through it and never reach `events`, hidden or not (`instrLog` still mirrors upsets, snapshots and replays while not hidden). `setHidden(true)` removes every INSTR-sourced event already in `events` and logs how many. `applyPreset` stops a replay and clears `state.drill` before `initSim`, so an armed drill cannot inject during the synchronous run-forward. `can()` is bypassed only while `applyReplayDue` is applying a journal entry (`this._replayApplying`), not for live input during a replay; `replaying()` remains for display use. `startReplay` reports `NO ACTIONS RECORDED AFTER SNAPSHOT` when the plan is empty. Tests: six regression cases appended to `tests/app-instructor.test.js`.
- **Verification round 2 (2026-08-29)**: `src/models.js` bounds the R-310 hot spot physically (`hotSpotRise`): the release is linear up to `bedRelKnee` (300 C) and saturates smoothly at `bedRelMax` (600 C, full conversion of the feed), and a logistic light-off factor on the preheat (`bedLightOff` 200 C, width `bedLightW` 25 C) extinguishes the bed once a fuel trip drops the inlet temperature, so every catalyst-activity / magnitude combination the sliders offer trips, peaks below ~700 C and resets (nominal and default-drill trajectories are unchanged within a few degrees). `ESS.Instructor.makeSnapshot` throws on a non-finite number in P / L / V (`nonFinitePath` exported); `snapshotData` catches it, notes `SNAPSHOT REFUSED` and returns null, and `saveSlot` / `backtrackTick` / `applyPreset` skip a null snapshot. Drills are journaled: `startDrill` writes op `DRILL` (tag = drill id, `instr:true`) and `endDrill('ENDED BY INSTRUCTOR')` writes `DRILLEND`; `applyJournalEntry` re-applies both, so REPLAY reproduces a drill armed after the snapshot (the injection delay comes from the restored generator). A snapshot carries `drill` (`drillData()`: id, t0, ti, injected, m, stableFor) and `restoreSnapshot` rebuilds `state.drill` through `drillFromData()`. The instructor logon prompt accepts only `instr` / `mngr`; other passwords give INVALID PASSWORD and leave `state.sec` alone. `randomDrill` draws from the seeded generator. The trainee message "drill armed" is dropped while upsets are hidden (the instructor log keeps it, with the injection time); the DRILL IN PROGRESS banner remains, as it names no fault. Tests: six regression cases appended to `tests/app-instructor.test.js` (catAct × bedact sweep incl. the U3 preset, non-finite snapshot refusal, drill journal / replay / snapshot, instructor logon, seeded random drill, hidden drill arming); `tests/models.test.js` bedact case allows the Urgent alarm one sample after the trip.
- **Hazards**: a restore drops everything after the snapshot (by journal sequence, by sim time for history / events), including the trainee's journal entries (a replay copies them first); a replay is applied at the start of `step()` for entries whose time has come and ends at the end of the step that reaches the original time, so `replayToEnd()` stops exactly at the recorded end. Snapshots are JSON clones — keep P / L / V free of functions, NaN and `undefined` values that matter. Presets run the process forward synchronously (the batch presets take up to ~5000 steps). The `drift` fault is not cleared by `endDrill` (it is not a drill fault); `initSim` resets it with the process.

## v2 changes: B6-scorer-signatures-moc (2026-08-29)

- **New file** `src/training.js` (`ESS.Training`, loaded in the head after `instructor.js`, inlined by `tools/build-dist.py`): our own operator task inventory — `GROUPS` (8), `tasks()` (42 tasks with `id, group, label, drills[], features[]`), `coverage(doneSet)`, `coverageSummary(doneSet)`, `PASS_MARK` (80) and `PASS_LABEL` ("independent training threshold, not a vendor certification"), the training record helpers `addRecord(list, rec, cap=20)` / `recordFor(...)`, the message record `message(t, txt, {confirm, src})` / `pending(msgs)`, `SIGNED_ACTIONS` and `configChange(...)`. Sources: course-catalogue feature inventory and the 80 % mark (RESOURCES 2.12), message confirm and asset alarm disable (2.12), MOC "who changed what and when" (2.13), one event file for logins / actions / config changes (2.1), operator-based logon (2.3). No vendor task names.
- **New file** `tests/app-scorer-moc.test.js` (10 cases: breakdown sums to the score and the 80 % label; KPI-module load element; coverage ticks after handlers; CONFIRM message counting, blinking and confirm event with the operator name; sequence / interlock / instructor prompts; signature refusals and the signed event; every signed action (trip point, priority, tuning, OOS, snapshot restore during a drill); asset disable / re-enable incl. parking of new alarms and the indicator; every CONFIG change with old / new, the MOC view and count; logon name capture). `tests/app-alarms.test.js` OOS case now signs; `tests/app-instructor.test.js` accepts the confirm prompt text.
- **Scorer**: `scoreDrill(d, ans)` returns `ESS.Kpi.scoreDrill(...)` (`{score, pass, passMark, passLabel, breakdown[]}`; weights 20/25/20/15/10/10; alarm load = `ESS.Kpi.computeMetrics(alarmLog, {t0:d.t0, t1:d.tEnd}).per10min.avg`). `endDrill` stamps `d.tEnd`. `submitDebrief(d, ans)` scores, journals `DRILL <id> SCORE n/100 — PASS|NOT PASSED (80 % MARK)` with the operator name in `newV`, appends to `this.trainingRecords` (cap 20, survives `initSim`) and ticks `abn.drill` / `abn.pass`. The debrief grade is PASS / NOT PASSED at 80 (the 85/65 PROFICIENT / COMPETENT grades are gone).
- **Operator name**: state `oper` (default `'OPERATOR'`) and `dlgName`; the logon dialog has an Operator field (`dg.logonIsOper, operCur, name, nameChange`); `logon()` stores the upper-cased name with the level and journals `SECURITY LEVEL CHANGE — OPERATOR a → b`; `signOff()` is a method now. `operName()` is the getter. Every event carries `who` (`addEvent`); the Event Summary shows an Operator column (Station narrowed to 48 px).
- **Messages**: message records are `{id, t, txt, src, confirm, confirmed, confirmedBy, confirmT}` (`postMsg(txt, {confirm, src})`, cap 400); the four old `msgs.unshift` sites go through it. Confirm-required sources: instructor drill armed / ended (`INSTR`; the armed prompt is dropped while upsets are hidden, the ended one is not flagged then), `scmPrompts()` (from `stepU2`; CHARGE→HEATUP "CONFIRM CHARGE COMPLETE" and DRAIN→IDLE "CONFIRM BATCH COMPLETE", src `SCM202`, tracked by `this._lastPhase`, reset by `initSim` / `restoreSnapshot`), the TI216 interlock latch (`TI216`). `pendingMsgs()`, `confirmMsg(id)` (OPER+, OPERATOR event `MESSAGE CONFIRMED — txt` with the name in `newV`, ticks `msg.confirm` and `bat.confirm` for SCM202). renderVals: `cMsg, msgOn, msgBg, msgFg, msgGo` (status-bar MSG n, blinks with `blink`), `msgsR[].{needs, confirmed, confT, bg, fw, confCb}`.
- **Electronic signature**: `withSignature(desc, needLevel, fn)` → `can(need)` then dialog `{type:'esig', desc, need, fn}` (reuses `dlgPw` / `dlgReason`); `signAction()` accepts a level password ranking ≥ `need`, requires a reason, journals `E-SIGNATURE — desc — reason` (`newV` = `NAME (LEVEL)`), closes the dialog and runs `fn(reason)`; the dialog that was open when the signature was requested is kept as `dlg.prev` and `cancelSignature()` (bound to `dg.close` for the esig dialog) brings it back; refusals journal `SIGNATURE REFUSED — desc` (`INVALID PASSWORD` / `LEVEL x BELOW y`). During a journal replay (`_replayApplying`) `fn('REPLAY')` runs directly. Signed actions: `setOos(tag, cond, true)`, trip-point stores (`TP:`), priority cycle (`prioCb`), PID `K/T1/T2` stores, `toggleAssetAlarms`, `restoreSlot` while a drill is running (MNGR; the CONFIG entry is written after the restore because the restore truncates the journal). renderVals: `dg.{isEsig, esigDesc, esigNeed, esigWho, esigKey, esigGo}`.
- **Asset alarm disable** (MNGR, signed): `this.disabledAssets` (Set, reset by `initSim`), `assetDisabled(tag)`, `assetTags(id)`, `disableAssetAlarms(id, on, reason)`, `parkDisabled(tag, cond, by)` (engine `oos` with a spec, record flag `disabledBy='ASSET:<id>'` — a dedicated field because the engine clears `suppressedBy` outside DSUPR; SYSTEM event `COND DISABLED — ASSET:x`), `isDisabledRec(r)`, `toggleAssetAlarms(id)`. `raiseA` parks a raise on a disabled asset's sources **before** the engine sees it (`parkDisabled(tag, cond, by, spec)` creates a missing record straight in OOSRV from the raise arguments; the engine's raise on an OOSRV record only marks the condition live, so no horn, ALARM journal line or KPI alarm-log row is produced and re-enable annunciates it). The set travels with instructor snapshots (`snapshotData` → `disabledAssets[]`, `ESS.Instructor.makeSnapshot` carries it, `restoreSnapshot` → `restoreDisabledAssets(list)` rebuilds the set — deriving it from the records' `disabledBy` flags when a snapshot has no list — and returns to service any record flagged for an asset that is not disabled in the restored state, with a SYSTEM line). `setOos(..., false)` refuses a record flagged `disabledBy`. Alarm Summary: `DISABLE ASSET` / `ENABLE ASSET` button (`av.btns`), right-click on a location row (`av.tree[].ctx`, `onContextMenu`), tree label suffix `— ALARMS DISABLED`, rows and Point Detail alarm rows show state `DISABLED` with `[DISABLED — ASSET:x]`; status bar `ALARMS DISABLED: n` (`disOn, cDis, disT`); System Status panel `ALARM SYSTEM` (disabled assets and condition count, OOS count, config changes, drill results kept). Action menu item added.
- **MOC audit**: `configChange(src, what, oldV, newV, reason)` → event type `CONFIG` (`desc = what — reason`), `this.mocCount` (survives `initSim`). `configChange(..., {program:'SCM202'})` attributes the entry to an automatic program (`addEvent` meta `who` / `lvl='PROGRAM'`) and leaves `mocCount` alone — the sequence-driven phase limit sets use it, so a batch adds CONFIG rows stamped `SCM202 / PROGRAM`, not the trainee's name; the change-log title names how many such rows are listed but not counted (`isProgramEvent(e)`). Routed through it: tuning K/T1/T2, SPHILM/SPLOLM/OPHILM/OPLOLM/SAFEOP, target band, trip points, priorities, control action, deadband (`ALMDB`) and on-delay (`ALMDELAY`, new ENGR entry params on the Alarms tab: `dpt.almCfg[]`; a store clears `l._am`), phase sets (`applyPhaseSet`, was SYSTEM), palette (was SYSTEM), PV tracking (was OPERATOR), OOS / RTS (`COND SERVICE STATE`), asset disable / enable, signed snapshot restore. Event filter `CONFIG (MOC)`; `openChangeLog()` (command `MOC` / `CHANGES`, View menu and System Menu links) opens the Event Summary on that filter with the title `CHANGE LOG (MOC) — n configuration changes since startup` (`evTitle`, `mocCount`).
- **Coverage matrix**: `this.tasksDone` (Set, survives `initSim`), `taskDone(id)`; hooks in `nav` (by display), `goBack/goFwd`, `setUnit` (new; unit tabs and U1/U2/U3 commands), `parseCmd`, `openFp`, `silence`, `ackAlarm`, `ackKeys`, `shelveAlarm`, `unshelveAlarm`, `commentAlarm`, the ALARM HELP toggle, `setOos`, `setMode`, SP / OP / trip / tuning stores, `raiseLower`, `motorCmd`, `setPvTrack`, `seqCmd`, `confirmMsg`, trend group / span chips, event filter chips, `logon` / `signOff`, `signAction`, `openChangeLog`, assistant GO steps, `submitDebrief`, `disableAssetAlarms`. Dialog `coverage` (command `COVERAGE` / `TASKS`, Drills menu): `dg.{isCoverage, covGroups[].{name,count,rows[].{label,tick,fg,fw,drills,features}}, covSummary}`. Dialog `record` (command `RECORD` / `TRAINING`, Drills menu): `dg.{isRecord, recRows[].{t,oper,drill,score,res,fg,detail}, recEmpty, recOpers[].{oper,txt}, recNote}`. Debrief: `dg.{dbBreak[].{label,pts,note,fg}, dbPassT, dbGradeFg, dbOper}`.
- **Changed contracts**: `scoreDrill` returns an object (was a number); `dg.dbSubmit` calls `submitDebrief`; `setOos(tag, cond, true)` no longer takes effect until `signAction()`; `storeEntry` for `TP:` / `K` / `T1` / `T2` returns true immediately and applies after the signature (equal values are no-ops); phase-set, palette and PV-tracking events are type `CONFIG`; the debrief question weights come from `ESS.Kpi.DEFAULT_RUBRIC`.
- **Verification round 1 (2026-08-29)**: the disabled-asset set is part of the snapshot (both restore directions agree with the records; regression test in `tests/app-scorer-moc.test.js`); `raiseA` parks before annunciating, so an equipment trip alarm with no prior record on a disabled asset stays silent; the ALARM HELP toggle reads the flag before `setState` (the harness merges state in place); phase limit sets are `SCM202 / PROGRAM` CONFIG rows outside the MOC count; the signature dialog restores the dialog it replaced on cancel.
- **Hazards**: `dlg.fn` and `dlg.prev` are kept in React state — never snapshot `state.dlg`. Handlers that tick a coverage task after `setState` must capture the flag first: the browser runtime replaces `state`, the node harness merges in place. `restoreSnapshot` drops the `E-SIGNATURE` event of a signed restore (journal truncated to the snapshot time); the CONFIG entry written afterwards carries the name and reason. `tasksDone`, `trainingRecords` and `mocCount` are session-scoped instance fields, not React state, so a dialog showing them re-renders on the 2 Hz tick.
